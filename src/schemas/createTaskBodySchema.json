{
  "title": "createTaskParamsSchema",
  "type": "object",
  "properties": {
    "id": { "description": "Id of the task", "type": "string" },
    "description": {
      "description": "description of the task",
      "type": "string"
    },
    "priority": { "description": "priority of the task", "type": "number" }
  },
  "required": ["id", "description", "priority"]
}
